import { StateTree, StoreDefinition, GettersTree, DefineStoreOptions } from './types';
/**
 * Defines a `useStore()` function that creates or retrieves the store instance
 * when called.
 *
 * @param options
 */
export declare function defineStore<Id extends string, S extends StateTree, G extends GettersTree<S>, A>(options: DefineStoreOptions<Id, S, G, A>): StoreDefinition<Id, S, G, A>;
//# sourceMappingURL=store.d.ts.map